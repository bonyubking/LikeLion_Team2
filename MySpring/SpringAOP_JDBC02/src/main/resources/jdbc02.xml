<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xmlns:context="http://www.springframework.org/schema/context"
  xmlns:aop="http://www.springframework.org/schema/aop"
  xmlns:tx="http://www.springframework.org/schema/tx"
  xsi:schemaLocation="http://www.springframework.org/schema/beans
           https://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/context
           https://www.springframework.org/schema/context/spring-context.xsd http://www.springframework.org/schema/aop https://www.springframework.org/schema/aop/spring-aop.xsd http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx.xsd">

  <!--AppConfig 설정-->
  <context:property-placeholder location="classpath:db.properties"/>

  <!--DataSource Bean 등록-->
  <bean id="dataSource" class="org.springframework.jdbc.datasource.DriverManagerDataSource">
    <property name="driverClassName" value="${driver}"/>
    <property name="url" value="${url}"/>
    <property name="username" value="${username}"/>
    <property name="password" value="${password}"/>
  </bean>

  <!--JdbcTemplate 등록-->
  <bean id="jdbcTemplate" class="org.springframework.jdbc.core.JdbcTemplate">
    <property name="dataSource" ref="dataSource"/>
  </bean>

  <!--TransactionManager 등록-->
  <bean id="transactionManager"
    class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
    <property name="dataSource" ref="dataSource"/>
  </bean>

  <!--트랜잭션 어노테이션 대신 AOP 기반으로 트랜잭션 설정-->
  <!--트랜잭션 규칙 정의-->
  <!--
  public class EmpService {
    public void getEmpList() {}     // → 읽기 전용 트랜잭션 적용
    public void insertEmp() {}      // → REQUIRED 트랜잭션 적용
    public void updateEmp() {}      // → REQUIRED 트랜잭션 적용 (name="*"에 걸림)
}
  -->
  <tx:advice id="txAdvice" transaction-manager="transactionManager">
    <tx:attributes>
      <tx:method name="get" read-only="true"/> <!--get으로 시작하는 메소드는 읽기 전용으로 설정-->
      <tx:method name="insert" propagation="REQUIRED"/> <!--insert로 시작하는 메서드는 트랜잭션을 시작-->
      <tx:method name="*" propagation="REQUIRED"/>
    </tx:attributes>
  </tx:advice>

  <!--어떤 메소드에 트랜잭션을 적용할지 정의하는 aop 설정-->
  <aop:config>
    <aop:pointcut id="serviceMethod" expression="execution(* com.mydb.core.EmpService.*(..))"/>
    <aop:advisor advice-ref="txAdvice" pointcut-ref="serviceMethod"/>
  </aop:config>

  <!--트랜잭션 어노테이션 활성화-->
  <!--  <tx:annotation-driven transaction-manager="transactionManager"/>-->

  <!--aop 설정-->
  <bean id="logAspect" class="com.mydb.core.LogAspect"/>
  <aop:config>
    <aop:aspect ref="logAspect">
      <aop:before method="before" pointcut="execution(* com.mydb.core.EmpService.*(..))"/>
      <aop:after method="after" pointcut="execution(* com.mydb.core.EmpService.*(..))"/>
    </aop:aspect>
  </aop:config>

  <!--AOP 자동 프록시 생성-->
  <!--  <aop:aspectj-autoproxy/>-->

  <!--Repository, Service 수동 Bean 등록-->
  <bean id="empDao" class="com.mydb.core.EmpDao">
    <constructor-arg ref="jdbcTemplate"/>
  </bean>
  <bean id="empService" class="com.mydb.core.EmpService">
    <constructor-arg ref="empDao"/>
  </bean>

</beans>