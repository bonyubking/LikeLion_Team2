org.Springframework.http
-ResponseEntity<T> Http응답을 가진 객체 HTTP상태코드 헤더 바디값 제어
http응답을 완전제어 _200 성공, 400_실패시 구현할수 있는 코드 코스텀

public ResponseEntity<T> {
		Httpstatus status; 상태코드
		HttpHeaders headers;
		T body;
}

@GetMapping("/hello")
	public ResponseEintty<String> hello(){
	String message - "안녕";
	return ReponseEntity.ok().body(message);
	
}

ex02)

@GetMapping("/download")
	public ResponseEntity<byte[]> downloadFile(){
		byte[] filedata = = .....;
		HttpHeadres headers = new HttpHeadres();
		headers.add(, , filename = " ");
		
		return  ResponseEntity.ok().header(headers).body(filedata);

         }
		
@ExceptionHandler(MethodArguementNotValidException.class)

	public ResponseEntity<String> handleValidationException(MEthodArgumentNOtValidException ex){
		String errorMessage = ex.getBindingResult()
							.getAllErrors()
							.get(0)
							.getDefaultMessage();
		return ResponseEntity
				.badRequest()
				.body(errorMessage); // body T
}

//

3. DisPatcherServlet : 모든 요청으 ㅣ진입점, 요청 위임과 흐름(Controller 매핑) 등
		
		Controller에서 실행되는 값 전달, 리턴 ,MVC 로직 흐름등 관련한 정보 객체
		
4. /templates/error.html -> Spring boot에서 기본 error 페이지로 지정해서 사용

없을 경우에는 위치에서 가상 생성해서 리턴/ 명시하면 명시된 페이지로 매핑

5. 커스텀 예외처리 
 @RestController -> @ResccontrollerAdvice 클래스에서 Responsetity객체로 리턴하는 메소드를 예외 종류에 맞게 구현
 @Controller -> @controlleradvice 클래스에서 ModelAndVIew 객체로 리턴하는 메소드를 예외 종류에 맞게 구현
 @ErrorController 인터페이스를 implements 해서 사용자 예외 클래스 선언후 이벤트로 핸들링
 	/templates/error/404.html
 						500.html 각각!		
