1. jpa, h2 inject 확인
프로젝트 실행 후 http://localhost:8000/h2-console

2. User 클래스 추가한다. getter, setter 

3 .User @ 를 이용해서 entity 생성

4. UserRepository.java 생성

JpaRepositoryt<T,ID> 확장 코드 작성
	T : 엔티티 클래스 타입 User
	ID : PK 타입 id = int = Integer


5. UserController.java 생성


6. index.html 생성
 	th:object="${formModel} : 폼의 모든데이터를 담는 객체
		model.addAttribute("formModel",user)
	th:value="*{name}"
		-> formMODEL 객체의 name 필드값 가져옴
		-> formMOdel.getName()
  
  <th:each="obj : ${data}"> -> th.each="변수명 : $(컬렉션객체)"
    <th:text="${obj.id}">
    <th:text="${obj.name}">
  