1. 웹 서버 실행 후 가이드

Initializing SPring DISPATCHERSERVELET 'dispatcherServlet'
@REquestMAPPINg으로 웹 요청으로 받아서 적절한 컨트롤러 로 전달할 준비가 되었다.

2. WebApplicationContext[웹 어플리케이션] -> Tomcat port 8080 -> 포트 잡고 서버 실행
							-> DISPatcherServlet -> 
DispatgcherServelt : 웹 요청 처리하는 Front Controller
톰캣이 사용자의 요청 받으면 DispatcherServlet으로 전달된다.
@RequestMApping을 찾아서 컨트롤러에 라우팅한다.
*들어오는 모드 웹 요청을 가로채서 어떤 컨트롤러에 분배할지 결정, 최종 응답을 사용자에게 본내는 역할


====================
Test01: main에서 웹 서버 싫행

@SpringBootApplication
public class DemoApplication {
	public static void main(String[] args) {
		SpringApplication.run(DemoApplication.class, args);
	}

}

Test02: DemoAPPLIcation 코드 추가 한후 @REquestMAPping @RestController확인

	@RequestMapping("/")
	String home() {
		return "안녕";
	}
}

Test03 : DEMOCONTRoller를 새로 생성해서 MVC 패턴으로 분류 시작


@RestController
public class DemoController {
	@RequestMapping("/")
	String home() {
		return "안녕";
	}
}

Test04 : @restcontroller와 @controller 차이
@RestController : 반환값 그래도 HTTP 응답본문(JSON 등) 으로 보냄, 템플릿 사용 X


@Controller : 반환값을 뷰 이름으로 해석하고 해당 템플릿 찾아 렌더링.
@Controller
public class DemoController {
	@RequestMapping("/")
	String home() {
		return "index"; // index.html 템플릿 뷰를 렌더링
	}
}

spirng-boot-start-thymeleaf : src/main/resources/templates 를 자동 설정 해준다.
								템플릿 파일 확장자 기본 설정 .html
								컨트롤러에서 "index"로 리턴하는 것을 View Resolver를 자동 구성
								컨트롤러에서 Model 객체를 통해 데이터를 뷰로(HTML)로 전달할 수 있고, <th:>태그를 지원

test05 : org.springframework.ui.Model 객체 확인.
 -> 컨트롤러에서 View(HTML) 데이터를 전달하기 위한 객체
@pathvariable(URL 경로 변수 바인딩) 사용하여 값을 전달하는 과정을 확인.
--> @RequestMappinig("/{num}")
	public String index(@PathVariable int num, Model model())
	
	
test06 : URL 직접 입력 방식 테스트
ModelAndView : 데이터도 담고 뷰 이름도 설정
addobject , setViewName;


test07 : GET + POST 방식으로 추가 코드 테스트

th:text = text 대입
th:value = 값 대입

GET/ -> 폼 출력 -> 입력용 리턴
POST/ -> 폼에서 숫자 입력 -> 합계 출력
GET /10 -> URL