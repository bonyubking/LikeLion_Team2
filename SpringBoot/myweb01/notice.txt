1. jpa, h2 inject 확인
프로젝트 실행 후 브라우저에서 http://localhost:080/h2-console DB 확인

2. User 클래스 추가 //getter, setter

3. User 클래스에 @ Entity 이용

4. UserRepository.java 생성
org.springframework.data.jpa.repository.JpaRepository<T,ID> 확장한 코드 작성
  T:  엔티티 클래스 타입
  ID : PK 타입

5. UserController.java 생성

6. index.html 생성
th:object="${객체이름}" : 폼의 모든데이터를 담는 객체
  model.addAttribute("객체이름", user);

th:value="*{name}" :  "*{속성명}"
  -> formModel 객체의 name 필드값을 가져옴
  -> formModel.getName()

th:each="obj:${data}" -> "변수명:${컬렉션 객체}"
th:text="${obj.id}"
th:text=${obj.name}