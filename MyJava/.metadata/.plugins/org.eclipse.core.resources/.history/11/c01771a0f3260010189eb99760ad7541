package com.typing.dao.Post;

import java.sql.SQLException;
import java.util.List;

import com.typing.model.dto.PostDTO;

public interface PostDAO {
	
	String insert = "INSERT INTO posts(CONTENT, CREATED_AT, USER_ID) VALUES (?,?,?)";
	String selectAll = "SELECT post_id, user_id, title, created_at FROM posts ORDER BY created_at DESC";
	String selectById = "SELECT * FROM posts WHERE post_id = ?";
	
    // 게시글 목록을 시간순으로 가져오기
    List<PostDTO> getPostsByTime() throws SQLException;

    // 게시글 ID로 게시글 가져오기
    PostDTO getPostById(int postId) throws SQLException;

    // 게시글 작성
    void createPost(PostDTO postDTO) throws SQLException;
}
